apiVersion: v2
name: broken-chart
description: This is intentionally broken chart for testing
type: application
version: 1.0.0
appVersion "1.0.0"   # Missing colon - Invalid YAML syntax
dependencies:
  - name: missing-dependency
    version: 999.999.999  # Non-existent version
    repository: https://non-existent-repo.example.com
invalid_yaml_here [
  this will cause: parsing errors
]
maintainers
  - name Test User  # Missing colon
    email: test@example.com